#lang info
(define collection "wyve")
(define deps '("base"))
(define pkg-desc
  "Wyve — contracts for the optimizer. Objective-C grammar, LLVM IR backend, conversational compilation.")
(define version "0.1")
(define pkg-authors '(O6lvl4))
