<!-- description: SPIR-V / NVPTX — the same contracts where they matter most -->
# GPU targets

Alias/effect contracts map directly onto GPU kernels (restrict, shared
memory tiling). The Scopes-inspired dual-backend idea: one contract
language, CPU and GPU lowerings. After @parallel.
